# Backend Environment Variables

# Server
HOST=0.0.0.0
PORT=8000

# Model Configuration
MODEL_NAME=efficientnetv2_rw_s
MODEL_NUM_CLASSES=2
INPUT_SIZE=224
# MODEL_WEIGHTS_PATH=weights/best_model.pt  # Default is auto-resolved

# CORS (comma-separated, use * for all origins in development)
BACKEND_CORS_ORIGINS=["*"]
